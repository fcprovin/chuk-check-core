version: 0.0
os: linux
files:
  - source:  /
    destination: /home/ec2-user/app/git/chuk-check-core/
file_exists_behavior: OVERWRITE

permissions:
  - object: /
    pattern: "**"
    owner: ec2-user
    group: ec2-user
